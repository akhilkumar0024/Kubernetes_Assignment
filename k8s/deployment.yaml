apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-app-deployment
  namespace: react-app-namespace
  labels:
    app: react
spec:
  replicas: 5
  selector:
    matchLabels:
      app: react-app
  template:
    metadata:
      labels:
        app: react-app
    spec:
      containers:
      - name: react-app-container
        image: akhilkumar119/react-app:v2
        ports:
        - containerPort: 80
          protocol: TCP
